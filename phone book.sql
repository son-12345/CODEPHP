CREATE TABLE contacts_table  (
                                 id INT PRIMARY KEY AUTO_INCREMENT,
                                 name VARCHAR(250),
                                 phone VARCHAR(250)
);
